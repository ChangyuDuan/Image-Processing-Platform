# 说明文档

## 项目规划
本项目旨在构建一个强大的图像处理与数据集分析平台，支持单图分析和数据集宏观分析。

## 实施方案
- 前端：HTML5 + ECharts + Bootstrap，实现美观的数据可视化。
- 后端：FastAPI，处理并发图片分析与特征提取。
- 算法：OpenCV, skimage, onnx (MobileNetV3) 等进行多维度特征计算。

## 进度记录
- **[2026-03-24] 数据集宏观分析优化**
  - [x] 在 `app/profiler.py` 的 `_analyze_rationality` 中增加计算数据集所有图片平均宽度 (`avg_width`) 和平均高度 (`avg_height`) 的逻辑。
  - [x] 在 `app/profiler.py` 的 `_analyze_rationality` 中增加计算最大/最小宽度 (`max_width`, `min_width`) 和最大/最小高度 (`max_height`, `min_height`) 的逻辑。
  - [x] 在 `app/templates/dataset_analysis.html` 中新增一个尺寸统计的 ECharts 柱状图展示平均、最大和最小宽高。
  - [x] 在总结报告表格中的“合理性”维度下，补充说明平均、最大和最小宽高的数值指标。
